[tools]
"cargo:cargo-llvm-cov" = "latest"
rust = "latest"

[tasks.test]
run = "cargo test"

[tasks.run]
run = "cargo run"

[tasks.coverage]
run = "cargo llvm-cov --all-features --workspace --ignore-filename-regex 'target/'"
